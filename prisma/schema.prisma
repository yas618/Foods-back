generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
}

model Food {
    id          Int      @id @default(autoincrement())
    nome        String
    description String
    price       Float
    category    String
    available   Float
    createdAt   DateTime @default(now())

    @@map("Food")
}
